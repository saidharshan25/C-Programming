#include <stdio.h>
void printNumbers(int n)
{
    if (n > 50) {
        return; 
    }
    printf("%d ", n); 
    printNumbers(n + 1);
}
void main() 
{
    printf("First 50 numbers are:\n");
    printNumbers(1);
}