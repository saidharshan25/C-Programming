#include <stdio.h>

void main() {
    int i,length=0;
    char a[30];
    printf("Enter a string:");
    scanf("%s",a);
    while(a[i]!='\0')
    {
        i++;
        length++;
    }
    printf("\n length of the string is:%d",length);
}
